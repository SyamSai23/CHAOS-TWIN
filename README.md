# Chaos Twin

Local-first tool that scans a codebase, builds a dependency graph, and simulates failure blast radius.

## Tech (v1)
- Backend: FastAPI (Python)
- Frontend: React + TypeScript (Vite)
- DB: Postgres (Docker)