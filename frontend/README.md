# Frontend (Vite + React + TypeScript)

## Run

```bash
npm install
cp .env.example .env
npm run dev
```

Default URL:
- `http://127.0.0.1:5173`

Environment:
- `VITE_API_BASE_URL` (default expected: `http://127.0.0.1:8000`)

Main project documentation is in the root `README.md`.
